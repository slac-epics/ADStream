#
# pluginStats.substitutions
#

file NDStats.template
{
	{
    TS_PORT         = "Stats\$(N)_TS",
    PORT            = "Stats\$(N)",
    P               = "$(CAM):",
	R				= "$(PORT):",
    ADDR            = "\$(ADDR=0)",
    TIMEOUT         = "\$(TIMEOUT=1)",
    NDARRAY_PORT    = "\$(PLUGIN_SRC)",
    NDARRAY_ADDR    = "\$(NDARRAY_ADDR=0)",
    RATE_SMOOTH		= "\$(RATE_SMOOTH=0.0)",
    TSE				= "\$(TSE=-2)",
	HIST_SIZE		= "\$(HIST_SIZE=256)",
    NCHANS		    = "\$(NCHANS=2048)",
	}
}

